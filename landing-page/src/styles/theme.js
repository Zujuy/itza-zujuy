// src/styles/theme.js
const theme = {
    primaryColor: "#333",        // Color para textos principales
    secondaryColor: "#555",      // Color para textos secundarios
    accentColor: "#007acc",      // Color de acento (botones, enlaces)
    backgroundColor: "#fafafa",  // Fondo general
    headerFont: "'Montserrat', sans-serif",
    bodyFont: "'Lato', sans-serif",
    spacing: "1rem",
  };
  
  export default theme;
  